# You cannot alter the element of the tuple once declared.

# t = (1,2,3,4,7,8,5,9,6)
# t1 = () #Empty Tuple
# t1 = (1,) # Tuple with single element always require comma at the end of the element otherwise it won't be considered as an single element tuple
# t1 = (1) # This won't be considered as tuple with single element because it doesn't have comma after the element.

# print(t1)